{
  "name": "braintrust-conversation-lambda",
  "version": "1.0.0",
  "description": "Multi-turn conversation Lambda with Braintrust tracing",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "deploy": "npm run build && cd dist && zip -r ../function.zip . && cd .. && aws lambda update-function-code --function-name braintrust-conversation-lambda-dev --zip-file fileb://function.zip --profile sandbox"
  },
  "dependencies": {
    "braintrust": "^3.3.0",
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/aws-lambda": "^8.10.133",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "braintrust",
    "lambda",
    "tracing",
    "conversation",
    "multi-turn"
  ],
  "author": "Braintrust Support Toolkit",
  "license": "MIT"
}
